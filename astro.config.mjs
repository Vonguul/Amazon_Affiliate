import { defineConfig } from 'astro/config';

// Update `site` once you've picked/connected a domain — Astro uses it to
// generate correct canonical URLs and Rich Pin / Open Graph metadata.
export default defineConfig({
  site: 'https://example.com',
  output: 'static',
});
